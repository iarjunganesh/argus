# Test data

A tiny synthetic data set in the layout `ARGUS_DATA_DIR` expects. `tests/conftest.py` points the
local data plane here, so tests never depend on data generated on a developer's machine. Every
name is invented.
