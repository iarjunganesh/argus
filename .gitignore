# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
env/
.env/

# Environment / secrets — NEVER commit these
.env
.env.local
.env.*.local
*.key
*.pem
*.pfx
secrets/

# Azure
.azure/
azd-env/
*.tfstate
*.tfstate.backup

# Jupyter
.ipynb_checkpoints/
*.ipynb

# IDE and agent-local settings (shared ones are .vscode/extensions.json and .claude/settings.json)
.vscode/settings.json
.claude/settings.local.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Node (diagram tooling now, web/ UI later)
node_modules/
web/.next/
package-lock.json

# Test / coverage / tool caches
.pytest_cache/
.coverage
coverage.xml
.ruff_cache/
.mypy_cache/
htmlcov/
*.log

# Generated synthetic data (large files — regenerate with make generate-data)
data/synthetic/*.jsonl
data/synthetic/*.csv
data/synthetic/*.parquet
data/synthetic/ocr_documents/

# Generated adverse-media payload export (written when Azure SDK is unavailable)
data/public/adverse_media_index_payload.jsonl
data/public/*.pdf

# Demo outputs
outputs/
reports/
data/*.jsonl

# Gradio local artifacts
.gradio/


# Local scratch and working plans — never commit
.tmp/

# Agent handoff notes shared between coding tools — never commit
/HANDOFF.md
